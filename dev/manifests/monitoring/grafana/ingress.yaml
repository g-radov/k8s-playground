---
apiVersion: "extensions/v1beta1"
kind: "Ingress"
metadata:
  name: "grafana-web"
  namespace: "monitoring"
spec:
  rules:
  - http:
      paths:
      - path: "/grafana"
        backend:
          serviceName: "grafana-web"
          servicePort: 3000
    host: "invalidbydefault.xyz"
...
